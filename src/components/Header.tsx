type Props = {
   openSidebarClick: () => void;
   title: string;
   newChatClick: () => void 
}

const Header = ({openSidebarClick, title, newChatClick}: Props) => {
    return (
        <header>
            
        </header>
    )
}

export default Header;